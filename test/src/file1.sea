include lib "stdio.h"

struct Pair {
    int x, y
}

fun constructor(int x = 0, int y = 0) -> Pair {
    return [x, y] as Pair
}

fun main() -> int {
    Pair pair1 = constructor(5, 6)
    c::printf("(%d, %d)\n", pair1.x, pair2.y)

    return 0
}
